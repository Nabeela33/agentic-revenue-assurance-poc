# Synthetic Revenue Assurance POC Dataset v2

Product fields added across systems. Transaction tables have 1000 records each. Attribute tables have multiple rows per record because they use name/value attributes.
